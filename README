This repository contains an analysis of climate data from Melbourne and Oxford to visualise visualise rainfall patterns in the cities.

To run this analysis use the following command:

```
Rscript src/combine-data-then-make-plot.R
```

The input data is in `data` and the results are in `out`.
